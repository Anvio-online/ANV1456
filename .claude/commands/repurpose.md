---
description: Turn one raw thought into content for every channel
argument-hint: '<the raw thought, note, or lesson>'
---

Turn this into content: $ARGUMENTS

Use the `anvio-social-media-manager` agent.

**First, extract the actual idea in one sentence** and check it survives two tests: is it specific enough that a competitor couldn't post it unchanged, and is it honest given that Anvio has no client stories or metrics to draw on? If the raw thought doesn't contain a publishable idea, say so and ask what's underneath it — **don't inflate a thin thought into six formats.**

Then: LinkedIn post, X post or thread, Instagram carousel, blog outline, newsletter, short-video script. One idea, six expressions — not six ideas.

Hold the voice rules: nothing from the banned list, no emoji outside LinkedIn/X, no invented statistics, no fake hooks, and no version that reads like a paste of another channel's text.

End with a sequencing note — which channel first, and the gap between. Same-day everywhere reads as automated.

Drafts go to `ops/content/`. I post, not you.
